// Package utils provides small string conversion helpers used by the malina
// FFI bindings. It mirrors bucky's pkg/utils so callers do not need to depend
// on golang.org/x/sys directly.
package utils
